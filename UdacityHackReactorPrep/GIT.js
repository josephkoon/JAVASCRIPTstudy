//GIT COMMANDS

git init //creates repository
git status //inspect contents of working directory
git add //adds files from the working directory to staging
git diff //difference between working directory and staging
git commit //permanently store file changes from staging area
git log //shows a list of all previous commits

git show HEAD //show the head commit
git checkout HEAD filename //restore filename to last commit
git reset HEAD filename //restores file to the HEAD commit. unstage.

git rest SHA //go back to a previous commit with first 7 character SHA for the previous commit
